# You May Copy And/Or DO Anything With This.
**Note, this was created by our AI model, any updates will be through reccomendating.**
<br>
This has a `.css` file meaning, dropping this into a `code .` would be reccomended.
<br>
# 
`code .` only opens a VS CODE file that is created using simple `powershell`
